--- tests/e2e/scopeweave.spec.js
+++ tests/e2e/scopeweave.spec.js
@@ -67,6 +67,7 @@
     await expect(page.getByTestId('summary-actual-progress')).toHaveText('1.05%');

     await expect(page.locator('.task-row')).toHaveCount(18); // 18 seeded items
+    await expect(page).toHaveTitle('ScopeWeave Planner');
   });

   test('disables export and gantt actions when there are no tasks', async ({ page }) => {
