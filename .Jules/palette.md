## 2026-06-01 - Add Focus Styles to Editor Fields
**Learning:** Missed focus-visible style patterns on row editor form fields (`input`, `select`) create an inconsistent UX compared to top meta-fields.
**Action:** Always reuse existing design system classes (like `meta-field` style patterns) when introducing new interactive form elements to ensure uniform accessibility and visual polish without writing duplicate custom CSS.
